\section*{<%= $conferencecontributions %>}
<% by_year(conferencecontributions) do |year, entries| %>
\subsection*{<%= year %>}
<% entries.each do |entry| %><% if entry == entries.last %><% if entry["annote"] %><%= entry["annote"] %>: <% end %><% if entry["coauthor"] %><%= $with %> <%= entry["coauthor"] %>, <% end %>\enquote{<%= entry["title"] %>}. <% if entry["conference"] %><%= entry["conference"] %>, <% end %><% if entry["institution"] %><%= entry["institution"] %>, <% end %><%= entry["location"] %> (<%= entry["month"] %>/<%= entry["year"] %>).<% else %><% if entry["annote"] %><%= entry["annote"] %>: <% end %><% if entry["coauthor"] %><%= $with %> <%= entry["coauthor"] %>, <% end %>\enquote{<%= entry["title"] %>}. <% if entry["conference"] %><%= entry["conference"] %>, <% end %><% if entry["institution"] %><%= entry["institution"] %>, <% end %><%= entry["location"] %> (<%= entry["month"] %>/<%= entry["year"] %>).\\[.25cm]<% end %><% end %><% end %>
  
  
